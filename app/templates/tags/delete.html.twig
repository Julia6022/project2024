{% include 'default/_form.html.twig' with {
    page_title: 'title.tags_delete'|trans({'%id%': tags.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'tags_index',
} %}